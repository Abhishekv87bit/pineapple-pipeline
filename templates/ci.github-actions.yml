# Generated by Pineapple Pipeline v1.0.0
# Template: ci.github-actions.yml
# Do not edit directly -- run `pineapple upgrade` to get latest version.
# ══════════════════════════════════════════════════════════
# Production Pipeline Template: GitHub Actions CI/CD
# ══════════════════════════════════════════════════════════
# Placeholders:
#   {{PYTHON_VERSION}}  - Python version (default: 3.12)
#   {{NODE_VERSION}}    - Node version (default: 20)
#   {{BACKEND_DIR}}     - Backend directory (default: backend)
#   {{FRONTEND_DIR}}    - Frontend directory (default: frontend)
#   {{TEST_COMMAND}}    - Test command (default: pytest)

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ── Lint & Type Check ──
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "{{PYTHON_VERSION}}"

      - name: Install linter
        run: pip install ruff

      - name: Lint backend
        run: ruff check {{BACKEND_DIR}}/

  # ── Backend Tests ──
  test-backend:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "{{PYTHON_VERSION}}"
          cache: pip

      - name: Install dependencies
        working-directory: {{BACKEND_DIR}}
        run: pip install -r requirements.txt

      - name: Run tests
        working-directory: {{BACKEND_DIR}}
        run: {{TEST_COMMAND}}
        env:
          TESTING: "true"

  # ── Frontend Build ──
  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "{{NODE_VERSION}}"
          cache: npm
          cache-dependency-path: {{FRONTEND_DIR}}/package-lock.json

      - name: Install dependencies
        working-directory: {{FRONTEND_DIR}}
        run: npm ci

      - name: Build
        working-directory: {{FRONTEND_DIR}}
        run: npm run build

  # ── Docker Build (verify it compiles) ──
  docker:
    runs-on: ubuntu-latest
    needs: [test-backend, build-frontend]
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker images
        run: docker compose build

  # ── LLM Eval (only on main, costs money) ──
  # Uncomment when GAP-PPL-004 is closed
  # llm-eval:
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   needs: [test-backend]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Set up Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: "{{PYTHON_VERSION}}"
  #     - name: Install dependencies
  #       working-directory: {{BACKEND_DIR}}
  #       run: pip install -r requirements.txt && pip install deepeval
  #     - name: Run LLM evals
  #       working-directory: {{BACKEND_DIR}}
  #       run: deepeval test run tests/test_llm_eval.py
  #       env:
  #         DEEPEVAL_API_KEY: ${{ secrets.DEEPEVAL_API_KEY }}
